# Dev-only dependencies -- needed to run tests/, not to run the scraper
# itself. Install with:
#   pip install -r requirements-dev.txt
#
# (installs on top of requirements.txt, doesn't replace it)

pytest
